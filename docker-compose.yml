version: '3.8'

services:
  bot:
    build: .
    container_name: spotify_telegram_bot
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./db:/app/db
      - ./temp:/app/temp
    env_file:
      - .env
